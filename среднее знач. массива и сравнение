function betterThanAverage(classPoints, yourPoints) {
let sum = 0;
    let count = 0;  
        for (let num of classPoints) {
          sum += num;
          count++;
        }
        let a = sum / count
        
        if (a > yourPoints) {
            return false
        } else {
            return true
        }
}
